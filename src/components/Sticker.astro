---
export interface Props {
  emoji?: string;
  className?: string;
  style?: string;
}
const { emoji = '✨', className = '', style = '' } = Astro.props;
---
<span class={`inline-block pointer-events-none select-none ${className}`} style={style} aria-hidden="true">{emoji}</span>
